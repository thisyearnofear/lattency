These SQL migrations are archival. They provisioned the retired Aurora
PostgreSQL backend (PostGIS geography columns, the cafe_speed_stats
materialized view, next-auth session tables). The app now runs entirely on
Base44 — see the "Base44 backend" section of AGENTS.md.

Kept for historical reference only. They are not applied by any script and
have no effect on the running system.
